{
  "_schema": "<diagram-id>: {commit, token, url, status, source, static_check}",
  "_status_enum": ["pending", "ready", "stale"],
  "state-machine-3.2": {
    "commit": "941a271",
    "token": null,
    "url": null,
    "status": "pending",
    "source": "data/design-lark-chart/state-machine.mmd",
    "static_check": "data/design-lark-chart/state-machine.check.json",
    "note": "Local StaticCheck PASS (errors=0, warnings=0, nodeCount=42, connectorCount=29). Awaiting human to provision Feishu whiteboard token via `/插入 → 画板` in the DESIGN.md docx, then run `lark-cli whiteboard +update --whiteboard-token <token> --input_format mermaid --source @data/design-lark-chart/state-machine.mmd` to populate."
  }
}
